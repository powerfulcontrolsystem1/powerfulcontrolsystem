Carpeta nativa de Cursor para este repositorio.

Propósito:

- Mantener una capa de gobernanza/skills para Cursor sin eliminar el sistema existente de GitHub Copilot (`copilot-instructions.md` y `.github/agents/`).

Contenido:

- `AGENTS.md`: declara `pcs-agente-go` como coordinador por defecto y enlaza el protocolo.
- `skills/`: skills por rol.
- `rules/`: reglas persistentes (si se crean).
